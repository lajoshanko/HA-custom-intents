language: hu
intents:
  HassGetState:
    data:
    # hőmérséklet
      - sentences:
          - "(Mi|Mennyi) a hőmérséklet a [<area>[ban][ben]]"
          - "(Mi|Mennyi) a <name> (hőmérséklet[e])"
        response: one
        requires_context:
          domain: sensor
          device_class: temperature
        slots:
          domain: sensor
          device_class: temperature
        # páratartalom
      - sentences:
          - "(Mi|Mennyi) a páratartalom a [<area>[ban][ben]]"
          - "(Mi|Mennyi) a <name> (páratartal[om][ma])"
        response: one
        requires_context:
          domain: sensor
          device_class: temperature
        slots:
          domain: sensor
          device_class: temperature
